### P40 completion note (a real demo)

- Three more real runs, five minutes' drive around each demo town, gave the towns' own OSM observations: cafés, betting shops, yoga studios, premium grocers, and green space, measured along the walking network today. Overpass answered two on the first try, gave Welwyn a 504 and then a 429, and answered on the third attempt after a pause; the skill's "rerun the identical command" advice held. `fixtures/demo/evidence.json` now carries those fifteen observations, keyed to the fixture ids the photos and tests already use, with the real node coordinates.
- The rail, housing, and street-care inputs are real and cited. Housing: the median of every standard-price flat sale in HM Land Registry Price Paid Data for each postal town, August 2023 to July 2026 (433, 806, and 972 sales), basis `transformed`, with the note that Price Paid Data does not record bedrooms. Street care: Defra's 2024/25 and 2023/24 incidents per 1,000 people for Welwyn Hatfield, Dacorum, and Windsor and Maidenhead, read from the published local-authority sheet, basis `measured`. Rail: journeys assembled from Trainline's published average journey time and Wikipedia's off-peak service pattern, with access, wait, and last mile as agent estimates, so the whole journey is `agent_inferred` at confidence 0.5; reliability is ORR's moving annual average, fetched by the ORR command (whose table calls the Thameslink operator "Greater Thameslink Railway"; the first attempt with the usual name failed and the retry was two cache hits). The import path was proved on the real Hemel run first: rail with `--performance`, then housing, then street care, each previewed and executed, three sibling bundles.
- The demo profile's limit moves from 65 to 70 minutes so the real journeys show both states: Maidenhead (67) and Welwyn (65) pass, Hemel (72) is outside. Welwyn's 65 is exactly on the line. The public demo is now 18 measured, 3 transformed, and 3 agent-inferred facts, none synthetic; the build passes the ORR file into the rail merge and dates the bundle today.
- Windsor and Maidenhead's fly-tipping rate halves between the two years, and Defra's 2023/24 row records every incident under one land type, which reads as a reporting change rather than cleaner streets. The schema has nowhere to say so on the place, and a descriptive `reporting_basis` would trigger the "does not report all incidents" warning, which is a different claim. It is noted here instead.
- The synthetic fixture moves to `fixtures/synthetic/` and the Python tests point at it, because they lean on its visit audits, stale audits, local reports, and small samples; the contract test round-trips both fixtures. The viewer's copy and tests follow the real demo: "three real towns, cited evidence", Maidenhead on top, a basis test that looks for Measured, Transformed, and Agent-inferred rather than Synthetic. README screenshots refreshed.

